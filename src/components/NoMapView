import React, { Component } from 'react';



class  noMapView extends Component {

    state = {
        show: false,
        timeout: null
    }

    componentWillMount =() => {
        let timeout = window.setTimeout(this.showMessage, 1000);
        this.setState({timeout});
    }

    render() {
        return (
            <div>

            </div>
        );
    }
}

export default No;